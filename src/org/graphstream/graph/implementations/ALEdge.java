package org.graphstream.graph.implementations;

public class ALEdge extends AbstractEdge {
	protected ALEdge(String id, AbstractNode source, AbstractNode target,
			boolean directed) {
		super(id, source, target, directed);
	}
}
